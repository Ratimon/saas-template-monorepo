# -----------------------------------------------------------------------------
# Backend – development env example
# Copy to backend/.env.development.local (or backend/.env) and set values.
# Load order: .env.development.local then .env (see config/GlobalConfig.ts).
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Basic
# -----------------------------------------------------------------------------
SITE_NAME=Content OS
SENDER_EMAIL_ADDRESS=noreply@example.com

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=3000
FRONTEND_DOMAIN_URL=http://localhost:5173
BACKEND_DOMAIN_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# API
# -----------------------------------------------------------------------------
API_PREFIX=/api/v1

# -----------------------------------------------------------------------------
# CORS (optional; comma-separated extra origins)
# -----------------------------------------------------------------------------
# ALLOWED_FRONTEND_ORIGINS=

# -----------------------------------------------------------------------------
# Auth
# -----------------------------------------------------------------------------
# Disable new user registration when true
DISABLE_REGISTRATION=false
# Dev only: relaxes auth cookie/header behavior over HTTP. Production: set NOT_SECURED=false or omit (default false).
NOT_SECURED=true
# Secret for signing organization invite links (required for invite-by-email).
# Fallback: JWT_SECRET if INVITE_TOKEN_SECRET is not set.
INVITE_TOKEN_SECRET=
# JWT_SECRET=

# -----------------------------------------------------------------------------
# OAuth (optional – omit or leave empty to disable a provider)
# -----------------------------------------------------------------------------
# Google redirect URI: ${BACKEND_DOMAIN_URL}${API_PREFIX}/auth/oauth/google/callback
OAUTH_GOOGLE_CLIENT_ID=
OAUTH_GOOGLE_CLIENT_SECRET=
# GitHub
OAUTH_GITHUB_CLIENT_ID=
OAUTH_GITHUB_CLIENT_SECRET=
# Generic OIDC (e.g. Keycloak, Auth0)
# OAUTH_GENERIC_AUTH_URL=
# OAUTH_GENERIC_TOKEN_URL=
# OAUTH_GENERIC_USERINFO_URL=
# OAUTH_GENERIC_CLIENT_ID=
# OAUTH_GENERIC_CLIENT_SECRET=
# OAUTH_GENERIC_SCOPE=openid profile email

# -----------------------------------------------------------------------------
# Email (verification, welcome)
# -----------------------------------------------------------------------------
EMAIL_ENABLED=true
# When true, use local SES mock (e.g. aws-ses-v2-local) for email
IS_EMAIL_SERVER_OFFLINE=true

# -----------------------------------------------------------------------------
# Supabase (required for auth and DB)
# -----------------------------------------------------------------------------
PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# -----------------------------------------------------------------------------
# AWS (SES for local/dev email)
# -----------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# -----------------------------------------------------------------------------
# Resend (production email)
# -----------------------------------------------------------------------------
RESEND_SECRET_KEY=

# -----------------------------------------------------------------------------
# Sentry (error monitoring)
# -----------------------------------------------------------------------------
SENTRY_DSN=
SENTRY_ENABLED=true

# -----------------------------------------------------------------------------
# Cache (memory or redis; used for module_configs etc.)
# -----------------------------------------------------------------------------
CACHE_PROVIDER=memory
CACHE_DEFAULT_TTL=300
CACHE_LOG_HITS=true
CACHE_LOG_MISSES=true
CACHE_CHECK_PERIOD=60
CACHE_USE_CLONES=false
CACHE_ENABLED=true
CACHE_ENABLE_PATTERNS=true

# Redis (when CACHE_PROVIDER=redis)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_PREFIX=app:cache:
REDIS_MAX_RECONNECT_ATTEMPTS=10
REDIS_ENABLE_OFFLINE_QUEUE=true
REDIS_USE_SCAN=true

# -----------------------------------------------------------------------------
# Rate limiting (global + auth; set RATE_LIMIT_ENABLED=false to disable)
# Global limit: requests per window per IP. In development the code default is
# 1000/hour; in production it's 30/hour. Override here if needed.
# -----------------------------------------------------------------------------
RATE_LIMIT_ENABLED=true
RATE_LIMIT_WINDOW_MS=3600000
RATE_LIMIT_MAX=60

# Auth routes (stricter, 15 min window)
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_MAX=50